Primero tienen que agregar arriba #include <string.h>

char filename[255];
sprintf(filename,"Tamaños_L=32.txt");
FILE *fp=fopen(filename,"w");

Estas tres lineas les crean el archivo txt.

Entonces supongamos que ustedes ahora tienen dos punteros Y y X de N componentes cada uno y quieren graficar Y vs X. Entonces hacen lo siguiente

for(i = 0; i < N; i++)
  {
  fprintf(fp,"%lf %lf",*(X+i),*(Y+i));
  }

fclose(fp);
cierran el archivo.

Esto les genera un txt con dos columnas, una es el vector X y la otra es el vector Y. Este formato está bueno porque entonces para abrirlo con python
lo único que tienen que hacer es:

import numpy as np

data = np.loadtxt("Tamaños_L=32.txt")
X = data[:,0]
Y = data[:,1]

Y ahí ya tienen los vectores para graficar.
